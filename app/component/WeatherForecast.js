"use client";

export default function WeatherForecast(props) {
  const wfinfo = props.wfinfo;
  console.log(wfinfo);
  const localTime = props.localTime;
  console.log(localTime);

  return <></>;
}
